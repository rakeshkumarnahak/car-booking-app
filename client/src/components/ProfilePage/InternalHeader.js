import React from "react";
import "./InternalHeader.css"; // Import the CSS file for the header styling

const Header = () => {
  return <div className="internal-header">User Profile</div>;
};

export default Header;
